# Delay freshly published versions to reduce exposure to rapid package compromise.
min-release-age=2

# Keep dependency resolution on the npm registry unless explicitly reviewed.
allow-remote=none
allow-git=none
allow-file=root
allow-directory=root

# Fail targeted rebuilds when a dependency install script has not been reviewed.
strict-allow-scripts=true
