---
---

Bootstrap the 1.0 release-candidate and Changesets workflow. The package
version and release notes are already prepared as `1.0.0-rc.1`, so this
checkpoint intentionally applies no additional semantic-version bump.
